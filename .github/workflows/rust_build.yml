name: Rust Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        BUILD_TARGET: [release]
    steps:
      - uses: actions/checkout@v5

      - name: Ensure stable toolchain
        run: rustup default stable

      - name: Output Rust versions
        run: |
          rustup --version
          rustc --version
          cargo --version

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build binaries in "${{ matrix.BUILD_TARGET }}" mode
        # Backend only: the Tauri manager crate needs GTK/WebKit system libs
        # this Ubuntu job doesn't install; it gets its own workflow later.
        run: cargo build --profile ${{ matrix.BUILD_TARGET }} -p vrc-ytdlp
